name: check
on:
  pull_request:
  push:
    branches: [main]

jobs:
  flake-check:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5
      - uses: cachix/install-nix-action@v31
      - run: nix flake check
