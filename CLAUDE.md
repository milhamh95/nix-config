# nix-config

Personal nix-darwin config. Two macs (`mbp`, `mac-desktop`), one user
(`milhamh95`), flake-parts + home-manager + sops-nix, built on
[denful/den](https://github.com/denful/den).

@AGENTS.md
